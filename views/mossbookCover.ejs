<!-- 🔙 Back to Your Own Profile -->
<div style="margin: 1rem 0;">
    <a href="/user/profile">
      <button style="background-color: #ffccf9; border: none; padding: 0.5rem 1rem; border-radius: 10px; font-weight: bold; cursor: pointer;">
        ⬅️ Back to My Profile
      </button>
    </a>
  </div>


<div class="moss-page" style="text-align: center;">
    <h2 style="font-family: 'Comic Sans MS', cursive; color: #3e6043;">📔 Your Mossbooks</h2>
  
    <!-- Create a new Mossbook -->
    <form action="/mossbook/<%= mossbook._id %>/cover" method="POST" enctype="multipart/form-data">
      <input name="title" value="<%= mossbook.title %>" />
      <input type="file" name="coverImageUpload" />
      <button type="submit">Save Cover</button>
    </form>
    
    
      
      <% if (mossbooks && mossbooks.length > 0) { %>
        <% mossbooks.forEach(book => { %>
          <div style="margin: 2rem auto; max-width: 400px; text-align: center; padding: 1rem; border: 3px dashed #bde0fe; border-radius: 1rem;">
            <h3><%= book.title %></h3>
            <% if (book.coverImage) { %>
              <img src="<%= book.coverImage %>" style="max-height: 120px; border-radius: 1rem; margin: 0.5rem 0;" />
            <% } %>
            <div style="margin-top: 0.5rem;">
              <a href="/mossbook/<%= book._id %>/cover">🎨 View Cover</a> |
              <a href="/mossbook/<%= book._id %>/page/0">📖 Open</a>
            </div>
          </div>
        <% }) %>
      <% } else { %>
        <p style="text-align:center;">You haven’t made any Mossbooks yet 🌿</p>
      <% } %>